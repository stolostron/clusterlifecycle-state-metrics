kind: Deployment
apiVersion: apps/v1
metadata:
  name: clusterlifecycle-state-metrics
  namespace: open-cluster-management
spec:
  template:
    spec:
      containers:
      - name: clusterlifecycle-state-metrics
        image: quay.io/open-cluster-management/clusterlifecycle-state-metrics:REPLACE_IMAGE
