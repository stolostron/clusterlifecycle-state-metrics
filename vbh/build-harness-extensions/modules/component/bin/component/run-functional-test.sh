#!/bin/bash

#The parameters are:
# ${1} is the image generated by the build.sh
# ${2} is the image generated by the build-coverage.sh if implemented.

echo "Please specify a COMPONENT_TYPE when running component/test/functional to select the type of component you're building, or implement your own run-functional-tests.sh and set COMPONENT_BUILD_COMMAND to that file's path.";
exit 1;
