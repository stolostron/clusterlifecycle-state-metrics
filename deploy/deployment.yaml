kind: Deployment
apiVersion: apps/v1
metadata:
  name: ocm-state-metrics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ocm-state-metrics
  template:
    metadata:
      labels:
        app: ocm-state-metrics
    spec:
      serviceAccountName: ocm-state-metrics
      containers:
      - name: ocm-state-metrics
        image: quay.io/open-cluster-management/ocm-state-metrics:2.2.0-PR1-3e9d4417784c01a1a48a8153b91f03fe0f23e983
        imagePullPolicy: IfNotPresent
        args:
          - "--port=8443"
          - "--telemetry-port=8444"
          - "--tls-crt-file=/var/run/ocm-state-metrics/tls.crt"
          - "--tls-key-file=/var/run/ocm-state-metrics/tls.key"
        volumeMounts:
          - mountPath: "/var/run/ocm-state-metrics"
            name: certs
            readOnly: true
  
      imagePullSecrets:
      - name: multiclusterhub-operator-pull-secret
      volumes:
        - name: certs
          secret:
            secretName: ocm-state-metrics-cert